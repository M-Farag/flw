tasks:
    - data:
        - replace
        - mina
        - munchy
    - data:
        - count
        - munchy
    - data: #This task is not implemented yet, but it is to support the idea of replacing data in CSV columns
        - replace
        - (column_name,old_value,new_value)
        - (column_name,old_value,new_value)


